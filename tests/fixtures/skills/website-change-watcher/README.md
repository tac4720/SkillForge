# website-change-watcher

Fixture skill for detecting deterministic changes in a local page snapshot.

- fetch the local watcher page
- compare before and after content hashes
- emit only the minimal notify payload fields
